# 服务名称
spring:
    application:
        name: bgs-star
    cloud:
        config:
            uri: @config.server.url@
            profile: @config.profile@
            label: master
            discovery:
                enable: true
                service-id: bgs-config-server
# 服务端口号
server:
    port: 11001
    
#logging:
#    level:
#        root: warn
#        name:
#            myc:
#                bgs: debug
#        org:
#            springframework:
#                web:
#                    client:
#                        RestTemplate: debug

##Eureka 相关配置
#eureka:
#    client:
#        service-url:
#            defaultZone: http://localhost:10000/eureka/
##        # 是否从其他的服务中心同步服务列表
##        fetch-registry: false
#        # 是否把自己作为服务注册到其他服务注册中心
#        register-with-eureka: true

# Hystrix 熔断器配置
hystrix:
    command:
        default:
            execution:
                isolation:
                    thread:
                        timeoutInMilliseconds: 5000