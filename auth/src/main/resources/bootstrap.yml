# 服务名称
spring:
    application:
        name: bgs-auth
    cloud:
        config:
            uri: http://localhost:10001/
            profile: local
            label: master
            discovery:
                enable: true
                service-id: bgs-config-server
# 服务端口号
server:
    port: 11000