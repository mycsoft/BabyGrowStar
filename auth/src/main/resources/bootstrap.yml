# 服务名称
spring:
    profile: @profiles.active@
    application:
        name: bgs-auth
    cloud:
        config:
            uri: @config.server.url@
            profile: @config.profile@
            label: master
            discovery:
                enable: true
                service-id: bgs-config-server
# 服务端口号
server:
    port: 11000